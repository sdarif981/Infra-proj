version: "3"

services:

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env

  frontend:
   build:
    context: ./frontend
    args:
      - VITE_API_URL=http://backend:5000
   ports:
    - "3000:80"